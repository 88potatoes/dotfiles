---
description: Show recent PRs, stacked PR hierarchy (bottom of stack), CI status, and action items
argument-hint: "[all | repo]"
---

Use the prs skill to inspect and present recent PRs and stacked PR chains. Arguments: `$ARGUMENTS`.
